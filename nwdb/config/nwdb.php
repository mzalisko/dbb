<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Dead-drop publisher
    |--------------------------------------------------------------------------
    |
    | "local" пише конверти на диск deaddrop (директорія, що віддається
    | нейтральним статик-доменом через rsync/mount за Tailscale).
    | "s3" — заглушка до додавання aws sdk (R2/MinIO).
    |
    */

    'publisher' => env('NWDB_PUBLISHER', 'local'),

    'deaddrop' => [
        'disk' => 'deaddrop',
        'path' => env('NWDB_DEADDROP_PATH', storage_path('app/deaddrop')),
        // Публічний URL нейтрального статик-домену (без trailing slash)
        'base_url' => env('NWDB_BASE_URL', 'https://example-static.test'),
        // Підкаталог на статик-хості, де лежать конверти
        'prefix' => env('NWDB_DEADDROP_PREFIX', 'assets'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Центральний підписний ключ Ed25519 (base64)
    |--------------------------------------------------------------------------
    |
    | Генерується через `php artisan nwdb:make-signing-key`.
    | Секрет ніколи не потрапляє у білди плагінів і не логується.
    |
    */

    'signing' => [
        'public' => env('NWDB_SIGN_PUBLIC', ''),
        'secret' => env('NWDB_SIGN_SECRET', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | Параметри білдів плагінів
    |--------------------------------------------------------------------------
    */

    'plugin' => [
        // Версія коду шаблону плагіна (не payload-версія)
        'version' => '1.0.0',
        // Базовий інтервал wp_cron, секунди: рандомізується в цьому діапазоні
        'cron_interval_min' => 3300,
        'cron_interval_max' => 4500,
    ],

];
